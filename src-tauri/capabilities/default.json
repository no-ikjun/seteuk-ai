{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "stronghold:allow-initialize",
    "stronghold:default"
  ],
  "include": ["plugin:store", "plugin:stronghold"]
}
